App As appinfo:
    BackEnabled: =true
    OnStart: |+
        =ClearCollect(
            Tabs,
            {Name: "Open"},
            {Name: "In Progress"},
            {Name: "Resolved"},
            {Name: "Closed"}
        );
        Set(
            uparrow,
            false
        );
        Set(
            downarrow,
            true
        );
        Set(
            uparrow1,
            false
        );
        Set(
            downarrow1,
            true
        );
        Set(
            uparrow2,
            false
        );
        Set(
            downarrow2,
            true
        );
        Set(
            uparrow3,
            false
        );
        Set(
            downarrow3,
            true
        );
        Set(
            uparrow4,
            false
        );
        Set(
            downarrow4,
            true
        );
        Set(
            uparrow5,
            false
        );
        Set(
            downarrow5,
            true
        );
        Set(
            uparrow6,
            false
        );
        Set(
            downarrow6,
            true
        );
        Set(
            uparrow7,
            false
        );
        Set(
            downarrow7,
            true
        );
        ClearCollect(
            colstages,
            {
                value: "20",
                Completed: true
            },
            {
                value: "40",
                Completed: true
            },
            {
                value: "80",
                Completed: true
            },
            {
                value: "100",
                Completed: false
            },
            {
                value: "Full",
                Completed: false
            }
        );
        ClearCollect(
            RadioOption,
            {Option1: "Low"},
            {Option2: "Medium"},
            {Option3: "High"}
        );
        Set(Checkb,Blank());
        Set(Checkb1,Blank());
        Set(Checkb2,Blank());
        Set(Checkb3,Blank());
        Set(Checkb4,Blank());
        Set(Checkb5,Blank());
        Set(Checkb6,Blank());
        Set(Checkb7,Blank());
        Set(Checkb8,Blank());
        Set(Checkb9,Blank());
        Set(Checkb10,Blank());
        Set(Checkb11,Blank());
        Set(Checkb12,Blank());
        Set(Checkb13,Blank());
        Set(Checkb14,Blank());
        Set(Checkb15,Blank());
        Set(Checkb16,Blank());
        Set(Checkb17,Blank());
        Set(Checkb18,Blank());
        Set(Checkb19,Blank());
        Set(Checkb20,Blank());
        Set(Checkb21,Blank());
        Set(Checkb22,Blank());
        Set(Checkb23,Blank());
        Set(Checkb24,Blank());
        Set(Checkb25,Blank());
        Set(Checkb26,Blank());
        Set(Checkb27,Blank());
        Set(Checkb28,Blank());
        Set(Checkb29,Blank());
        Set(Checkb30,Blank());
        Set(Checkb31,Blank());
        Set(Checkb32,Blank());
        Set(Checkb33,Blank());
        Set(Checkb34,Blank());
        Set(Checkb35,Blank());
        Set(Checkb36,Blank());
        Set(Checkb37,Blank());
        Set(Checkb38,Blank());
        Set(Checkb39,Blank());
        Set(Checkb40,Blank());
        Set(Checkb41,Blank());
        Set(Checkb42,Blank());
        Set(Checkb43,Blank());
        Set(Checkb44,Blank());
        Set(Checkb45,Blank());
        Set(Checkb46,Blank());
        Set(Checkb47,Blank());
        Set(Checkb48,Blank());
        Set(Checkb49,Blank());
        Set(Checkb50,Blank());
        Set(Checkb51,Blank());
        Set(Checkb52,Blank());
        Set(Checkb53,Blank());
        Set(Checkb54,Blank());
        Set(Checkb55,Blank());
        Set(Checkb56,Blank());
        Set(Checkb57,Blank());
        Set(Checkb58,Blank());
        Set(Checkb59,Blank());
        Set(Checkb60,Blank());
        Set(Checkb61,Blank());
        Set(Checkb62,Blank());
        Set(Checkb63,Blank());
        Set(Checkb64,Blank());
        
        
        
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

